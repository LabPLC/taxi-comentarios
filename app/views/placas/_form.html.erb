<%= form_for(@placa) do |f| %>
  <% if @placa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@placa.errors.count, "error") %> prohibited this placa from being saved:</h2>

      <ul>
      <% @placa.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :numero %><br>
    <%= f.text_field :numero %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
